server:
  port: 8084

spring:
  application:
    name: storage-service
  servlet:
    multipart:
      enabled: true
      max-file-size: 25MB
      max-request-size: 50MB

app:
  upload:
    local-path: ${UPLOAD_LOCAL_PATH:./uploads}
  gcp:
    bucket-name: ${GCP_BUCKET_NAME:schoolappraisal-attachments}